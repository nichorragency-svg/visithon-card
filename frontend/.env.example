# --- Visithon card app (required on Vercel) ---
REACT_APP_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_anon_key

# --- Optional legacy API (Admin Panel `/admin/*` only) ---
# Leave unset for card-only deployments; the Digital Card flows use Supabase.
# Dev default is http://127.0.0.1:8000 when NODE_ENV=development if unset.
# REACT_APP_API_BASE_URL=https://your-api.example.com
